import React from 'react';
import ReactDOM from 'react-dom/client';
import KendoBracket from './KendoBracket.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <KendoBracket />
  </React.StrictMode>,
);